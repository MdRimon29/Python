#Set are used to store multiple items in a single varibale
#sets are unordered,unchangable,donot allow duplicates
#we use {'x','y'} syntax for set

set1={'Rimon','Hasib','Sojeb'}
set2={'Rimon'}

#print(set1) #print a set

#intersect=set1 & set2   #we can intersect two set by using & operator(intersect is all the items in common)
#print(intersect)

#union=set1 | set2   #we can calculate the union by using | operator
#print(union)

#difference= set1 - set2 #we can find out the difference from one set to another by using - operator
#print(difference)

#superSet= set1 >set2    #if set1 is superset for set2 or not
#print(superSet)

#subSet= set1 <set2    #if set1 is subset for set2 or not
#print(subSet)

#print(len(set1))    #count the length for set1

#print (list(set1))  #we can create a list of a set

#print('Rimon' in set1)  #we can use in operator for check a element is in the set or not

#set3={'num1','num2','num1'}
#print(len(set3)) #set cannot have multiple items 